;; Set command used for `grep-find' (default of `find' runs Windows
;; `find' command, which is not what we want). [EAC]
;; TODO: We need to figure out how to get all the args to show up too.
;; (grep-apply-setting 'grep-find-command "gfind")

